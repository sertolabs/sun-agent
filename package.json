{
  "name": "veramo-sun",
  "private": "true",
  "version": "1.0.0",
  "description": "Minnimal deployment of @veramo/cli server to Heroku",
  "repository": "https://github.com/uport-project/daf-deployments",
  "author": "jason.healy@mesh.xyz",
  "license": "Apache-2.0",
  "scripts": {
    "start": "NODE_ENV=production yarn veramo server -p $PORT --config=./config/agent-heroku.yml",
    "dev": "yarn veramo server --config=./config/agent-local.yml"
  },
  "dependencies": {
    "@veramo/cli": "^1.0.1"
  }
}
